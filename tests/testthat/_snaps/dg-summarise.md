# dg_summarise() errors on invalid input

    Code
      dg_summarise(datasets = 42)
    Condition
      Error in `dg_summarise()`:
      ! `datasets` must be a list of <tibble>s, a character vector or NULL.

