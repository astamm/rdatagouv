% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dg-problems.R
\name{dg_problems}
\alias{dg_problems}
\title{Return the parsing problems of a downloaded table}
\usage{
dg_problems(x)
}
\arguments{
\item{x}{A table returned by \code{\link[=dg_pull_dataset]{dg_pull_dataset()}} or \code{\link[=dg_refetch]{dg_refetch()}}.}
}
\value{
A data frame with columns \code{row}, \code{col}, \code{expected} and \code{actual}
(one row per parsing issue), or \code{NULL} if there were none.
}
\description{
Returns the data frame of parsing issues that \href{https://vroom.tidyverse.org/}{vroom}
encountered while reading a table downloaded with \code{\link[=dg_pull_dataset]{dg_pull_dataset()}} or
\code{\link[=dg_refetch]{dg_refetch()}}. These are vroom's "parsing issues" (rows that could not be
converted to the inferred column type, e.g. a mostly-padded ISO date column
holding a few non-padded values like \code{2021-7-01}).
}
\details{
The noisy per-cell warnings themselves are suppressed by default during a
pull; use this accessor to inspect what happened. The problems live in the
\code{rdatagouv_problems} attribute of the table. Returns \code{NULL} when the table
carries no recorded problems (or when \code{x} is an ordinary data frame).
}
\examples{
dg_problems(iris)
}
