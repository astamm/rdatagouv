% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dg-find-topics.R
\name{dg_find_topics}
\alias{dg_find_topics}
\title{Find topics (themes) on data.gouv.fr}
\usage{
dg_find_topics(q = NULL, n = 20, elements = FALSE)
}
\arguments{
\item{q}{Optional full-text query matched against topic names/descriptions
(server-side). Defaults to \code{NULL}, meaning no filter.}

\item{n}{Maximum number of topics to return. Defaults to \code{20}. Set to \code{Inf}
to retrieve as many as the API allows.}

\item{elements}{Whether to fetch each topic's \code{elements} subsection (one
extra request per topic, an N+1 crawl) to fill the \code{n_datasets},
\code{n_dataservices} and \code{n_reuses} counts exactly. Defaults to \code{FALSE}, in
which case \code{n_elements} is the topic's declared total and the breakdown
counts are \code{NA}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per matching topic and columns:
\itemize{
\item \code{id} — the stable, unique 24-hex topic id (passable to the \code{topic}
argument of \code{\link[=dg_find_datasets]{dg_find_datasets()}}). Always non-\code{NA}.
\item \code{name} — the topic's display name.
\item \code{slug} — the URL-friendly slug.
\item \code{description} — the topic's description.
\item \code{tags} — comma-joined tags, or \code{NA}.
\item \code{featured} — whether the platform features this topic.
\item \code{n_elements} — number of elements (datasets, reuses,
dataservices, ...) grouped under the topic, from the API.
\item \code{n_datasets}, \code{n_dataservices}, \code{n_reuses} — per-kind counts, only
when \code{elements = TRUE} (else \code{NA}).
}
}
\description{
Searches the platform's themes via the v2 \code{topics/search} endpoint and
returns a tibble with one row per matching topic, including its stable
24-hex \code{id}. That \code{id} is what you pass to the \code{topic} argument of
\code{\link[=dg_find_datasets]{dg_find_datasets()}} to restrict a catalog search to one theme. (data.gouv
does not resolve topic names/slugs to ids inside \code{dg_find_datasets()}, so
this finder is the way to discover a theme and get its id.)
}
\details{
Useful when you want to \emph{discover} which curated themes exist and how many
elements (datasets, reuses, dataservices...) they group, before narrowing a
search — e.g. browse "Mobilité", "Environnement", "Énergie".
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Browse the curated themes.
dg_find_topics(n = 5)[, c("id", "name", "n_elements")]

# Narrow a catalog search to one theme once you have its id.
mob <- dg_find_topics(q = "mobilité")
dg_find_datasets(topic = mob$id[1], n = 10)
\dontshow{\}) # examplesIf}
}
