% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dg-schema.R
\name{dg_schema}
\alias{dg_schema}
\title{Documented schema of a parsed table's columns}
\usage{
dg_schema(x)
}
\arguments{
\item{x}{Either a table returned by \code{\link[=dg_pull_dataset]{dg_pull_dataset()}} or \code{\link[=dg_refetch]{dg_refetch()}}
(its \code{id} attribute is read automatically) or a table address string: the
canonical URI \verb{https://www.data.gouv.fr/datasets/<dataset_id>#<resource_id>}
(or \code{...#<resource_id>/<file>} for a file inside a ZIP), or a legacy
composed id of the form \verb{<dataset_id>::<resource_id>} /
\verb{<dataset_id>::<resource_id>::<file>}, as readable with \code{\link[=dg_table_id]{dg_table_id()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per column and the columns \code{name},
\code{title}, \code{description}, \code{type} and \code{example} (where the schema provides
them; absent entries are \code{NA}), or \code{NULL} (with a message) if the resource
has no declared schema. The schema's own \code{title} and \code{name} are attached as
the attributes \code{schema_title} and \code{schema_name}.
}
\description{
Returns the declared data schema of a table's columns: the per-variable
\code{fields} recorded by the dataset producer. Because data.gouv attaches a schema
only as a \emph{pointer} (\code{schema$name} / \code{schema$url}), this resolves that pointer
against \href{https://schema.data.gouv.fr}{schema.data.gouv.fr} and returns the
human-readable column documentation (\code{name}, \code{title}, \code{description}, \code{type})
that the schema carries — the information needed to judge whether a variable
really means what a statistical exploration assumes.
}
\details{
This is a \emph{supplement} to \code{\link[=dg_pull_dataset]{dg_pull_dataset()}}: the table itself comes from the
main API; the schema is read from the producer's declared data specification.
Only resources that carry a schema pointer have documentation; resources
without one return \code{NULL} with a message. Use \code{dg_list_datasets()} (column
\code{has_schema}, or the \code{schema_only} argument) to target schema-documented
tables in the first place.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
tbl <- dg_pull_dataset("62c5961ff0013fb71d7278e3")
dg_schema(tbl)
\dontshow{\}) # examplesIf}
}
