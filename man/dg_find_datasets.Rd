% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dg-find-datasets.R
\name{dg_find_datasets}
\alias{dg_find_datasets}
\title{Find datasets available on data.gouv.fr}
\usage{
dg_find_datasets(
  q = NULL,
  n = 1000,
  format = catalog_formats(),
  schema_only = FALSE,
  organization = NULL,
  geozone = NULL,
  access_type = NULL,
  license = NULL,
  tag = NULL,
  topic = NULL,
  granularity = NULL,
  last_update = NULL,
  producer_type = NULL,
  resources = FALSE
)
}
\arguments{
\item{q}{Optional full-text search query. When given, only datasets
matching \code{q} are returned (the API performs the search). Defaults to
\code{NULL}, meaning no filtering.}

\item{n}{Maximum number of datasets to return. Defaults to \code{1000}.
Set to \code{Inf} to retrieve as many as the API allows (capped at 10,000).}

\item{format}{Optional character vector of resource formats to keep. Only
datasets that have at least one resource in one of these formats are
returned. The v2 API matches multiple values when passed as repeated
parameters (a server-side union). Defaults to the full set of officially
tabular formats (\code{csv}, \code{csv.gz}, \code{xls}, \code{xlsx}, \code{parquet}).}

\item{schema_only}{Whether to keep only datasets that declare a data schema
(see \code{has_schema}). Defaults to \code{FALSE}. v2 has no boolean "declares any
schema" server-side filter, so this filters client-side on \code{has_schema},
which itself needs the per-dataset resource fetch. When \code{schema_only} is
set without \code{resources = TRUE}, this function forces \code{resources = TRUE}
(with an informative message about the extra requests) so the filter
actually runs.}

\item{organization}{Optional data producer, matched server-side. Pass either
the producer's \strong{24-hex \code{organization} id} (as shown in the
\code{organization} column of the returned tibble or on the dataset page), or
its exact \code{name} or \code{slug} — a human-readable value is resolved to its id
automatically via \code{\link[=dg_find_organization]{dg_find_organization()}} (only an exact match is
auto-resolved, so results stay reproducible; an ambiguous or unmatched
value stops with the candidate list). Note that, unlike v1, the v2 search
API itself only accepts the id (a raw slug yields zero matches), which is
why the package resolves names/slugs for you. Defaults to \code{NULL}.}

\item{geozone}{Optional territorial filter, passed as a territory code of
the form \code{"<scope>:<code>"}, e.g. \code{"country:fr"}, \code{"country-group:ue"},
\code{"country-subset:fr:metro"}, \code{"fr:region:..."}, \code{"fr:departement:974"},
\code{"fr:epci:..."}, \code{"fr:commune:75056"}, \code{"fr:arrondissement:..."},
\code{"fr:canton:..."}, \code{"fr:collectivite:..."}, \code{"fr:iris:..."} or \code{"poi:..."},
or the bare \code{"country"}/\code{"country-group"}/\code{"country-subset"} scope with an
omitted code for pan-national groupings. Accepted territory codes are
open-ended (any INSEE code for the relevant scope), so this argument is not
enumerated; only the format is validated. Defaults to \code{NULL}.}

\item{access_type}{Optional access filter. One of \code{"open"} (freely
downloadable) or \code{"restricted"} (access requires approval). Defaults to
\code{NULL}.}

\item{license}{Optional license filter, one of the exhaustive license slugs
\code{"lov2"}, \code{"notspecified"}, \code{"fr-lo"}, \code{"odc-odbl"}, \code{"other-at"},
\code{"cc-by"}, \code{"other-pd"}, \code{"cc-by-sa"}, \code{"other-open"}, \code{"odc-by"},
\code{"cc-zero"}, \code{"odc-pddl"}. Defaults to \code{NULL}.}

\item{tag}{Optional tag filter. Tags form an open vocabulary (dynamic
facets), so any free-form tag such as \code{"mobilite"} is accepted and is not
enumerated or validated. Defaults to \code{NULL}.}

\item{topic}{Optional topic filter, the \strong{24-hex \code{topic} id} of a theme
(found via \code{\link[=dg_find_topics]{dg_find_topics()}}). Only datasets grouped under that topic are
returned. Matched server-side as a single-valued filter, so pass exactly
one id. Topic ids form an open vocabulary (themes are created
dynamically), so this is not enumerated or validated. Unlike \code{organization},
a human-readable topic name/slug is not auto-resolved — use
\code{\link[=dg_find_topics]{dg_find_topics()}} to discover a theme and get its id. Defaults to \code{NULL}.}

\item{granularity}{Optional spatial granularity filter, one of the
exhaustive values \code{"other"}, \code{"fr:commune"}, \code{"country"}, \code{"fr:epci"},
\code{"fr:departement"}, \code{"poi"}, \code{"fr:region"}, \code{"fr:canton"},
\code{"country-group"}, \code{"country-subset"}, \code{"fr:collectivite"}, \code{"fr:iris"},
\code{"fr:arrondissement"}. Defaults to \code{NULL}.}

\item{last_update}{Optional update-recency filter, one of \code{"last_30_days"},
\code{"last_12_months"} or \code{"last_3_years"}. Defaults to \code{NULL}.}

\item{producer_type}{Optional producer-type filter, one of the exhaustive
values \code{"public-service"}, \code{"local-authority"}, \code{"company"},
\code{"not-specified"}, \code{"user"} or \code{"association"}. Defaults to \code{NULL}.}

\item{resources}{Whether to fetch each dataset's resources subsection
(one extra request per dataset) so the exact \code{n_resources}, \code{formats},
\code{has_table} and \code{has_schema} columns can be computed. Defaults to
\code{FALSE}, in which case those columns are \code{NA}. Automatically forced to
\code{TRUE} when \code{schema_only = TRUE} (see \code{schema_only}).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per matching dataset. The
\code{title} and \code{id} columns are always non-\code{NA}; the \code{id} column holds the
stable, unique dataset identifier used to address a dataset with
\code{\link[=dg_pull_dataset]{dg_pull_dataset()}}. When \code{resources = TRUE}, the columns also include
\code{n_resources} (number of files/resources), \code{formats} (a list-column
whose elements are the distinct file formats found among them), \code{has_table}
(whether at least one resource is
in a format this package can parse) and \code{has_schema} (whether at least one
resource carries a pointer to a declared data schema, whose per-variable
documentation is exposed by \code{\link[=dg_schema]{dg_schema()}}); these are \code{NA} when
\code{resources = FALSE} (the default) unless \code{schema_only = TRUE}, which forces
the fetch so \code{has_schema} is filled and the filter can run.
}
\description{
Collects the datasets published on the data.gouv.fr platform, searching the
catalog via the v2 \code{datasets/search} endpoint (the same one the web
interface uses). By default it returns the first \code{n} datasets; use \code{q} to
search titles and descriptions server-side instead of enumerating the whole
catalog.
}
\details{
Fetching \emph{every} dataset on the platform means paging through many thousands
of records in many HTTP requests and is both slow and fragile, so the
default is deliberately bounded. Set \code{n = Inf} to return as many matches as
the API allows. Note that data.gouv caps a search at 10,000 matches, so an
un-narrowed \code{n = Inf} crawl stops at that cap even though the platform holds
more. For large or infinite \code{n} the crawl scales its page size up (to ~250)
so a full 10,000-row crawl takes ~40 requests, not hundreds.

Because v2 search embeds rich per-dataset metadata inline, the returned
tibble includes columns such as \code{license}, \code{quality_score}, \code{views},
\code{access_type}, \code{frequency}, \code{temporal_start}/\code{temporal_end}, \code{archived} and
\code{featured} that help judge whether a dataset is worth pulling.

v2 search does NOT inline each dataset's resources (they are subsection
pointers), so the exact resource-based columns \code{n_resources}, \code{formats},
\code{has_table} and \code{has_schema} can no longer be computed without one extra
request per dataset (an N+1 crawl). By default these are \code{NA}; set
\code{resources = TRUE} to opt into the per-dataset resource fetch and fill them
exactly.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
datasets <- dg_find_datasets(n = 20)
head(datasets)

# Search server-side instead of downloading the whole catalog.
cycle <- dg_find_datasets(q = "vélo", n = 10)

# Only datasets that carry at least one parquet resource; the v2 API matches
# multiple formats as a server-side union.
compact <- dg_find_datasets(format = "parquet", n = 10)

# Only datasets with a declared schema (documented variables). `schema_only`
# forces the per-dataset resource fetch itself (~30s for n = 1000), so
# `resources = TRUE` is optional here.
documented <- dg_find_datasets(schema_only = TRUE, n = 10)

# Narrow by producer and territory. A producer may be given by its 24-hex
# id or by its exact slug/name (resolved for you), and by geozone.
fr <- dg_find_datasets(organization = "sncf",
                       geozone = "country:fr", n = 10)

# Only datasets grouped under one topic (find its id with dg_find_topics()).
mob <- dg_find_topics(q = "mobilité")
dg_find_datasets(topic = mob$id[1], n = 10)
\dontshow{\}) # examplesIf}
}
