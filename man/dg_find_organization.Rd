% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dg-find-organization.R
\name{dg_find_organization}
\alias{dg_find_organization}
\title{Search for organizations (data producers) on data.gouv.fr}
\usage{
dg_find_organization(q = NULL, n = 20)
}
\arguments{
\item{q}{Optional full-text query matched against organization names,
descriptions, etc. (server-side). Defaults to \code{NULL}, meaning no filter.
When \code{NULL} the most recently active/popular producers are returned.}

\item{n}{Maximum number of organizations to return. Defaults to \code{20}. Set to
\code{Inf} to retrieve as many as the API allows.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per matching organization and
columns:
\itemize{
\item \code{id} — the stable, unique 24-hex producer id (passable to the
\code{organization} argument of \code{\link[=dg_find_datasets]{dg_find_datasets()}}). Always non-\code{NA}.
\item \code{name} — the producer's display name.
\item \code{slug} — the URL-friendly slug.
\item \code{acronym} — the acronym, or \code{NA}.
\item \code{description} — the producer's description.
\item \code{datasets} — number of datasets the producer currently publishes.
\item \code{badges} — comma-joined badge kinds (e.g.
\code{"public-service, certified"}), or \code{NA}.
\item \code{business_number_id} — the French SIREN identifier when known, else
\code{NA}.
}
}
\description{
Searches the platform's producers via the v2 \code{organizations/search} endpoint
and returns a tibble with one row per matching organization, including its
stable 24-hex \code{id}. That \code{id} is exactly what you pass to the \code{organization}
argument of \code{\link[=dg_find_datasets]{dg_find_datasets()}} to restrict a catalog search to one
producer — or, more conveniently, you can pass a producer's exact \code{name} or
\code{slug} to \code{dg_find_datasets()} directly and it is resolved for you (see
\code{\link[=dg_find_datasets]{dg_find_datasets()}}).
}
\details{
Useful when you want to \emph{discover} which producers exist, how a producer's
name is spelled, or how many datasets it publishes — before narrowing a
search.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Who publishes rail/mobility data? (server-side ranked search)
orgs <- dg_find_organization(q = "SNCF")
orgs[, c("id", "name", "datasets")]

# Use the resolved id to narrow a catalog search to one producer.
datagouv <- dg_find_organization(q = "data.gouv")
open_data <- dg_find_datasets(organization = datagouv$id[1], n = 5)
\dontshow{\}) # examplesIf}
}
