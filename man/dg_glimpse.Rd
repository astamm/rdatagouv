% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dg-glimpse.R
\name{dg_glimpse}
\alias{dg_glimpse}
\title{Glimpse the metadata of a dataset on data.gouv.fr}
\usage{
dg_glimpse(id, table = NULL)
}
\arguments{
\item{id}{A dataset identifier (24-hex), a composed table id, or a table
returned by \code{\link[=dg_pull_dataset]{dg_pull_dataset()}}/\code{\link[=dg_refetch]{dg_refetch()}} (its \code{id} attribute is
read).}

\item{table}{Whether to also include the dataset's list of resources (from
the v2 resources subsection, one extra request per dataset). \code{NULL} or
\code{FALSE} (the default) skips the per-resource fetch; \code{TRUE} includes it.}
}
\value{
A list with the v2-inline metadata:
\itemize{
\item \code{quality}: a list with \code{score} (0-1) and boolean flags (\code{license},
\code{temporal_coverage}, \code{spatial}, \code{update_frequency},
\code{dataset_description_quality}).
\item \code{metrics}: views, resources_downloads, followers, discussions, reuses,
dataservices.
\item \code{context}: organization (name/slug/id), license, frequency,
temporal_coverage, spatial/granularity, access_type, archived, featured.
\item \code{resources} (only when \code{table = TRUE}): the list of resource objects.
}
}
\description{
Surfaces the dataset-level health and engagement metadata that the v2 API
embeds inline but the v1 fetch path does not expose — the bridge between
\emph{discover} (\code{\link[=dg_list_datasets]{dg_list_datasets()}}) and \emph{judge} (\code{\link[=dg_schema]{dg_schema()}}'s column
documentation). It reports the dataset's \code{quality} score and flags, its
\code{metrics} (views, downloads, followers, ...) and its context
(organization, license, frequency, temporal/spatial coverage, access type),
helping a user decide whether a dataset is worth pulling.
}
\details{
\code{id} composes naturally with the rest of the package: it may be a dataset id
(24-hex), a composed table id or a pulled table (whose \code{id} attribute is
read), so a dataset discovered or pulled elsewhere can be glimpsed directly.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
g <- dg_glimpse("6a6be5976a05df136d48fb7a")
g$quality
g$metrics
\dontshow{\}) # examplesIf}
}
